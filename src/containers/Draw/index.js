import React from 'react';
import { connect } from 'react-redux';

export class Draw extends React.PureComponent {
  render() {
    return (
      <div>
        <h1>Draw</h1>
      </div>
    );
  }
}

export default Draw;
